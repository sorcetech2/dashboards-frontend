// Vitest runs outside Next's Server Component compiler. The production
// `server-only` package is intentionally replaced by this no-op test module;
// the source modules still retain their runtime server-only boundary.
